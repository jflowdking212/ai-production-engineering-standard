# AI Engineering Audit — generated by audit mode, not hand-written

Run: point an agent at this repo and ask it to audit per
`AI_AGENT_INSTRUCTIONS.md` §3. It should overwrite this file with findings
grouped CRITICAL/HIGH/MEDIUM/LOW, each with file + why it matters, and should
NOT modify any application code while doing so.

(unpopulated — no audit has been run yet)
